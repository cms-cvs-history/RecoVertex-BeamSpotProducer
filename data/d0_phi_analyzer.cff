
module d0_phi_analyzer = BeamSpotAnalyzer
{
  untracked string OutputFileName = "analyze_d0_phi.root"

  PSet BSAnalyzerParameters = 
  {
	double MinimumPt = 2. // Gev/c
	int32 MaximumNtracks = 1000 // disable for the moment
	untracked string TrackCollection = "ctfWithMaterialTracks"
	bool WriteToDB = false // do not write results to DB
	bool RunAllFitters = false // run only default fitter
  }
  

}
