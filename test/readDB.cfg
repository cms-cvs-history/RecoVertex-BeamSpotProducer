process readDB = {

  include "FWCore/MessageLogger/data/MessageLogger.cfi"

  source = EmptySource { }
  
  untracked PSet maxEvents = {untracked int32 input = 2}

  # database output service
#  include "CondCore/DBCommon/data/CondDBCommon.cfi"

  # for sqlite use the following two lines
  #replace CondDBCommon.connect = "sqlite_file:NominalCollision.db"    
  # for devdb10 use the following three lines
  #replace CondDBCommon.connect = "oracle://cms_orcoff_int2r/CMS_COND_BEAMSPOT"
  #replace CondDBCommon.DBParameters.authenticationPath = "/afs/cern.ch/cms/DB/conddb"

  #es_source = PoolDBESSource { 
  #  using CondDBCommon
  #  VPSet toGet = { 
  #	  { string record = "BeamSpotObjectsRcd" string tag = "NominalCollision" } 
  #  }	 
  #}
  # for frontier comment lines for sqlite and devdb10 and include the following

  #es_source = PoolDBESSource {
  #       VPSet toGet = {
  #           {
  #               string record = "BeamSpotObjectsRcd"
  #               string tag = "SimpleGaussian"
  #           }
  #       }
  #       string connect =  "frontier://CoralDev/CMS_COND_BEAMSPOT"
  #       string timetype = "runtime"
  #       PSet DBParameters = {
  #           untracked int32 messageLevel=0
  #       }
  #}
  
  include "RecoVertex/BeamSpotProducer/data/BeamSpotNominalCollision_DevDB.cff" 

  module beamspot = BeamSpotFromDB {
  }
  
  path p = { beamspot }

}
